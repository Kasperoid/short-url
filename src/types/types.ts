export type responseUrlType = {
  result_url: string;
};

export type errorType = {
  message: string;
};

export type saveInput = {
  id: string;
  inputUrl: string;
  shortUrl: string;
};

export type linksScroll = {
  text: string;
  to: string;
};
