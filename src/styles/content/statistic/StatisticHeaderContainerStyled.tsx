import { Space, SpaceProps } from 'antd';
import { styled } from 'styled-components';

export const StatisticHeaderContainerStyled = styled(Space)<SpaceProps>`
  && {
    max-width: 400px;
    text-align: center;
  }
`;
